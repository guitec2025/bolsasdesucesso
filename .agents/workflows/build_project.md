# Build Project Workflow

Follow these steps to install dependencies and build the PageCraft AI project for production:

1. **Install Dependencies**:
   ```bash
   npm install
   ```

2. **Build Next.js Application**:
   ```bash
   npx next build
   ```
